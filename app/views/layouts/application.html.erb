<!DOCTYPE html>
<html>
  <%= render 'head' %>
  <body>
    <div class='layout__v-wrapper'>
      <div class='layout__site'>
        <%= render 'layouts/header' %>
        <%= render 'messages' %>
        <div class='layout__w-wrapper'>
          <%= yield %>
        </div>
      </div>
      <div class='layout__footer  menu--dark  menu--touch'>
        <%= render 'layouts/menu' %>
      <div>
    </div>
  </body>
</html>
