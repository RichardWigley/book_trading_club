<div class='menu'>
  <%= active_link_to accounts_show_path(current_account), class: 'menu__link', class_active: 'menu__link--active', active: [['accounts', 'contacts']] do %>
    <div class='menu__item'>
      <%= fa_icon 'user' %>
      Account
    </div>
  <% end %>
  <%= active_link_to offers_path, class: 'menu__link', class_active: 'menu__link--active' do %>
    <div class='menu__item'>
      <%= fa_icon 'share' %>
      Offer
    </div>
  <% end %>
  <%= active_link_to wants_path, class: 'menu__link', class_active: 'menu__link--active' do %>
    <div class='menu__item'>
      <%= fa_icon 'reply' %>
      Want
    </div>
  <% end %>
  <%= active_link_to trades_path, class: 'menu__link', class_active: 'menu__link--active', active: :exact do %>
    <div class='menu__item'>
      <%= fa_icon 'handshake-o' %>
      Trades
    </div>
  <% end %>
</div>

